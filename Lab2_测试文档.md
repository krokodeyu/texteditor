# Lab2 测试文档

## 1. 目标与范围

Lab2 要求在 Lab1 基础上新增统计模块与拼写检查，并修改/新增共 9 个命令（2 个修改 + 7 个新增）。同时评分项中明确要求提供自动化测试（不强制覆盖率）。

本项目测试策略分为两层：

- **Workspace/模块层单元测试**：对“确定性规则”和“错误处理”做精确断言（输出格式、边界条件、约束校验）。

- **CLI 随机命令测试（Fuzzing 风格）**：端到端随机生成命令序列，覆盖命令解析、状态机切换、撤销重做、跨文件操作，重点发现 panic、死锁、状态不一致等问题。该测试脚本已实现并记录每次会话日志，便于复现。

---

## 2. Lab2 需求要点与对应测试

### 2.1 `init`（修改）

需求：`init <text|xml> <file> [with-log]`，XML 初始化必须写 XML 声明在首行，根标签名固定 `root` 且 `id="root"`；若启用日志则通过根元素属性 `log="true"` 表示（XML 不支持 `# log` 注释）。

- **单元测试**：
  
  - `init xml` 创建内容可被解析，首行以 `<?xml ...?>` 开头，根元素 `root/id=root`；
  
  - `with-log` 时根元素带 `log="true"`。

### 2.2 `editor-list`（修改）+ 统计模块

需求：统计“当前会话”内每个文件成为活动文件的累计编辑时长，并在 `editor-list` 中以可读格式展示；时长格式包含 `≥24小时 -> X天Y小时`。

- **单元测试**：对四个区间分别断言：秒 / 分钟 / 小时+分钟 / 天+小时（尤其是 `1天3小时` 这种边界）。

- **随机 CLI 测试**：脚本在两条命令之间随机 sleep，用来触发“活动文件计时”的切换与累计逻辑。

### 2.3 XML 编辑命令（新增 6 个）

命令清单与适用对象：insert-before / append-child / edit-id / edit-text / delete-element / xml-tree（仅 XML 编辑器）。  
XML 约束：每个元素必须有唯一 id，且**不支持混合内容**（元素要么只有文本，要么只有子元素）。

- **单元测试**（建议覆盖的确定性点）：
  
  - `insert-before`：target 不存在、newId 冲突、尝试在根前插入的报错文本。
  
  - `append-child`：parent 不存在、newId 冲突、父元素已有文本导致“混合内容”报错。
  
  - `edit-id`：old 不存在、new 已存在、禁止修改根 id。
  
  - `edit-text`：元素不存在；元素有子元素时报“混合内容”相关错误。
  
  - `delete-element`：元素不存在；禁止删除根元素。

- **随机 CLI 测试（你已实现）**：  
  会随机生成上述 6 个 XML 命令，并维护“可能存在的 xml 元素 id 集合”以提高成功率（同时也保留大量无效输入覆盖错误处理）。

### 2.4 `xml-tree`（新增）

需求：以树形字符或缩进展示层级；显示全部属性（包括 id）；显示文本内容（如果有）。显示类命令不改变状态，不进入撤销栈。

- **单元测试**：断言输出包含必要信息（根、某个子节点、属性、文本）；同时验证调用 `xml-tree` 不影响 undo/redo（例如执行修改命令后记录栈深度，再调用 xml-tree，栈深度不变）。

### 2.5 `spell-check`（新增）

需求：对 `.txt` 检查全部文本；对 `.xml` **仅检查元素文本内容**，不检查标签名/属性名/属性值；输出示例里 XML 以“元素 id”为定位（如 `title1`、`author2`）。

- **单元测试**：对 XML 文本抽取逻辑做断言（只返回有直接文本的元素，label 使用 id，而不是 tag 计数）；这能稳定支撑后续 spell-check 的输出格式要求。

---

## 3. 随机命令（CLI fuzz）测试设计说明

你已实现脚本 `tests/random_test.py`，核心设计如下：

- 随机生成命令序列，包含 Lab1 命令 + Lab2 修改/新增命令，兼做回归测试。

- 每个会话在独立目录运行被测二进制，减少会话间相互影响。

- 向 stdin 逐条喂命令，并在命令间随机 sleep 以触发统计模块的计时累计。

- 输出记录：命令、stdout、stderr、退出码都会写入 `fuzz_runs/session_<i>.log` 以便复现与人工分析。

- XML 命令生成会维护 `xml_ids` 集合，用于生成 fresh id / 选择已有 id，提高 insert/edit/delete 类命令成功概率，同时也允许一定比例无效输入覆盖异常处理路径。

### 3.1 该 fuzz 测试的“判定标准”（Oracle）

当前脚本偏“健壮性”导向，最核心的通过标准是：

- 进程不崩溃（stderr 不出现 panic 等关键字）

- 会话能正常退出（exit code 合理）

- 日志可复现（同 seed、同 session 的命令序列固定）

建议你在文档中明确：**该测试主要捕获崩溃与状态机问题，不替代单元测试对输出格式/精确语义的断言**。

（如果你想增强 oracle，也可以在脚本末尾追加：对 xml 文件做一次 `xml-tree` 并检查输出包含 id/文本，或直接尝试解析保存后的 XML；并检测“混合内容”不会被生成出来——对应约束来自 Lab2。）

---

## 4. 运行方法

### 4.1 单元测试

```bash
cargo test
```

### 4.2 CLI 随机命令测试

```bash
cargo build
python tests/random_test.py
```

脚本会在项目根目录下生成 `fuzz_runs/`，每个 `session_<i>.log` 包含完整命令序列与输出，可直接复制命令序列用于复现。

---

## 5. 覆盖总结

- **确定性规则**（输出格式、边界条件、错误提示）：由 workspace 层单测保证（例如 `editor-list` 的 `≥24小时 -> X天Y小时` 、`init xml` 根元素约束 、`spell-check` XML 只检查元素文本且按元素 id 输出 ）。

- **系统健壮性**（命令解析、跨文件切换、undo/redo 与统计模块交互、异常路径）：由随机 CLI fuzz 覆盖，重点发现崩溃/死循环/状态不一致。
